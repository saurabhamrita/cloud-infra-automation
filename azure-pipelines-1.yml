trigger: none
pool: pipele_new

stages:
  - stage: init
    jobs:
      - job: init
        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'init'
            backendServiceArm: 'manoj bhai sp'
            backendAzureRmStorageAccountName: 'sss'
            backendAzureRmContainerName: 'fff'
            backendAzureRmKey: 'saurabh.tfstate'

